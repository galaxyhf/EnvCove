import Link from "next/link";
import { notFound } from "next/navigation";
import { Download, GitCompareArrows, Plus, Search, Upload } from "lucide-react";
import { and, desc, environments, eq, getDb, projects, secrets, secretVersions } from "@envvault/db";
import { createEnvironment, importSecrets, saveSecret } from "@/app/actions";
import { requireUser } from "@/lib/authorization";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { SecretListItem, SecretRow } from "@/components/secret-row";
export const dynamic = "force-dynamic";
export default async function Page({ params, searchParams }: { params: Promise<{ projectId: string }>; searchParams: Promise<{ env?: string; q?: string }> }) { const user = await requireUser(); const { projectId } = await params; const query = await searchParams; const db = getDb(); const [project] = await db.select().from(projects).where(and(eq(projects.id, projectId), eq(projects.userId, user.id))).limit(1); if (!project) notFound(); const envs = await db.select().from(environments).where(eq(environments.projectId, projectId)).orderBy(environments.name); const selected = envs.find((e) => e.id === query.env || e.slug === query.env) ?? envs[0]; const secretRows = selected ? await db.select().from(secrets).where(eq(secrets.environmentId, selected.id)).orderBy(secrets.key) : []; const versions = selected ? await db.select({ secretId: secretVersions.secretId, createdAt: secretVersions.createdAt }).from(secretVersions).innerJoin(secrets, eq(secretVersions.secretId, secrets.id)).where(eq(secrets.environmentId, selected.id)).orderBy(desc(secretVersions.createdAt)) : []; const q = query.q?.toLowerCase() ?? ""; const list: SecretListItem[] = secretRows.filter((s) => !q || s.key.toLowerCase().includes(q) || s.description.toLowerCase().includes(q)).map((s) => ({ id: s.id, key: s.key, description: s.description, updatedAt: s.updatedAt.toISOString(), versions: versions.filter((v) => v.secretId === s.id).map((v) => v.createdAt.toISOString()) })); const all = await db.select({ environmentId: secrets.environmentId, key: secrets.key }).from(secrets).innerJoin(environments, eq(secrets.environmentId, environments.id)).where(eq(environments.projectId, projectId)); const keys = [...new Set(all.map((x) => x.key))].sort(); return <div><div className="mb-7"><Link href="/dashboard/projects" className="text-xs text-muted-foreground">Projects /</Link><div className="mt-2 flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-2xl font-semibold">{project.name}</h1><p className="text-sm text-muted-foreground">{project.description || project.slug}</p></div><div className="flex gap-2"><Dialog><DialogTrigger asChild><Button variant="outline"><GitCompareArrows />Compare</Button></DialogTrigger><DialogContent className="sm:max-w-2xl"><DialogHeader><DialogTitle>Compare environments</DialogTitle><DialogDescription>Key presence only. Values stay hidden.</DialogDescription></DialogHeader><div className="overflow-auto"><table className="w-full text-sm"><thead><tr className="border-b"><th className="py-3 text-left">Key</th>{envs.map((e) => <th key={e.id} className="px-3">{e.name}</th>)}</tr></thead><tbody>{keys.map((key) => <tr key={key} className="border-b"><td className="py-3 font-mono text-xs">{key}</td>{envs.map((e) => <td key={e.id} className="text-center text-primary">{all.some((x) => x.key === key && x.environmentId === e.id) ? "✓" : "—"}</td>)}</tr>)}</tbody></table></div></DialogContent></Dialog><Dialog><DialogTrigger asChild><Button><Plus />New environment</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>Create environment</DialogTitle><DialogDescription>Isolate a new set of secrets.</DialogDescription></DialogHeader><form action={createEnvironment.bind(null, projectId)} className="space-y-4"><Label>Name</Label><Input name="name" required /><Label>Slug</Label><Input name="slug" required /><Label>Description</Label><Input name="description" /><Button className="w-full">Create</Button></form></DialogContent></Dialog></div></div></div><div className="mb-5 flex overflow-x-auto border-b">{envs.map((e) => <Link key={e.id} href={`?env=${e.id}`} className={`shrink-0 border-b-2 px-3 py-3 text-sm ${selected?.id === e.id ? "border-primary" : "border-transparent text-muted-foreground"}`}>{e.name}</Link>)}</div>{selected ? <Card><CardContent className="p-0"><div className="flex flex-col gap-3 border-b p-4 sm:flex-row sm:justify-between"><form className="relative max-w-sm flex-1"><Search className="absolute left-3 top-3 size-4 text-muted-foreground" /><Input name="q" defaultValue={query.q} className="pl-9" placeholder="Search keys" /><input type="hidden" name="env" value={selected.id} /></form><div className="flex gap-2"><Button asChild variant="outline" size="sm"><a href={`/api/environments/${selected.id}/export?filename=.env.local`}><Download />Export</a></Button><Dialog><DialogTrigger asChild><Button variant="outline" size="sm"><Upload />Import</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>Import .env</DialogTitle><DialogDescription>Existing keys create a new version.</DialogDescription></DialogHeader><form action={importSecrets.bind(null, selected.id)} className="space-y-4"><Textarea name="content" className="min-h-64 font-mono" required /><Button className="w-full">Import</Button></form></DialogContent></Dialog><Dialog><DialogTrigger asChild><Button size="sm"><Plus />Add variable</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>Add variable</DialogTitle><DialogDescription>Encrypted before storage.</DialogDescription></DialogHeader><form action={saveSecret.bind(null, selected.id)} className="space-y-4"><Label>Key</Label><Input name="key" required /><Label>Value</Label><Input name="value" type="password" required /><Label>Description</Label><Input name="description" /><Button className="w-full">Encrypt and save</Button></form></DialogContent></Dialog></div></div>{list.length ? list.map((s) => <SecretRow key={s.id} secret={s} environmentId={selected.id} />) : <p className="py-20 text-center text-sm text-muted-foreground">No variables found.</p>}</CardContent></Card> : <Card><CardContent className="py-20 text-center">No environments yet.</CardContent></Card>}</div>; }
