import Link from "next/link";
import { KeyRound } from "lucide-react";
function Code({ children }: { children: string }) { return <pre className="my-5 overflow-x-auto rounded-lg border bg-[#0c0c0c] p-4 font-mono text-[13px] leading-6 text-muted-foreground"><code>{children}</code></pre>; }
export default function Page() { return <div className="min-h-screen"><header className="sticky top-0 border-b bg-background/90 backdrop-blur"><div className="mx-auto flex h-16 max-w-5xl items-center justify-between px-5"><Link href="/" className="flex items-center gap-2 font-semibold"><KeyRound className="size-4 text-primary" />EnvVault Docs</Link><Link href="/dashboard" className="text-sm text-muted-foreground">Dashboard</Link></div></header><main className="mx-auto max-w-3xl px-5 py-14"><p className="font-mono text-xs uppercase tracking-widest text-primary">Documentation</p><h1 className="mt-3 text-4xl font-semibold">Ship without sharing .env files.</h1><p className="mt-4 text-lg leading-8 text-muted-foreground">Store encrypted variables and load them only for authorized developers and processes.</p><section className="border-b py-10"><h2 className="text-2xl font-semibold">Getting Started</h2><Code>{`npm install -g envvault\nenvvault login\ncd meu-projeto\nenvvault init\nenvvault pull\nnpm run dev`}</Code></section><section className="border-b py-10"><h2 className="text-2xl font-semibold">Login</h2><p className="mt-3 text-muted-foreground">Create a token in Dashboard → CLI Tokens. It is shown once and stored globally with restricted permissions.</p><Code>{`envvault login\nenvvault whoami`}</Code></section><section className="border-b py-10"><h2 className="text-2xl font-semibold">Initialize a Project</h2><p className="mt-3 text-muted-foreground">The interactive command creates a committable .envvault.json containing no secret values.</p><Code>{`envvault init\n\n{\n  "projectId": "project-uuid",\n  "environment": "development",\n  "output": ".env.local"\n}`}</Code></section><section className="border-b py-10"><h2 className="text-2xl font-semibold">Pulling Secrets</h2><Code>{`envvault pull\nenvvault pull --env production --output .env.production\nenvvault pull --force`}</Code></section><section className="border-b py-10"><h2 className="text-2xl font-semibold">Running Commands</h2><p className="mt-3 text-muted-foreground">Inject secrets directly into the child process without writing an environment file.</p><Code>{`envvault run npm run dev\nenvvault run node server.js`}</Code></section><section className="border-b py-10"><h2 className="text-2xl font-semibold">CLI Reference</h2><Code>{`login  logout  init  pull  run\nwhoami  projects  environments`}</Code></section><section className="py-10"><h2 className="text-2xl font-semibold">Security</h2><p className="mt-3 leading-7 text-muted-foreground">AES-256-GCM with unique IVs, server-owned master key, hashed passwords and CLI tokens, ownership checks, explicit sensitive endpoints, audit events, and HTTPS in production.</p></section></main></div>; }
